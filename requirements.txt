# GridIQ Backend Dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0           # PostgreSQL async driver
psycopg2-binary==2.9.9    # sync fallback
alembic==1.13.2           # database migrations
redis[hiredis]==5.0.8     # cache + pub/sub

# ── Time-series (TimescaleDB is PostgreSQL-compatible, no extra driver needed)
# For InfluxDB as secondary time-series store:
influxdb-client==1.44.0

# ── Message queue ──────────────────────────────────────────────────────────────
aiokafka==0.11.0          # async Kafka producer/consumer

# ── SCADA protocols ────────────────────────────────────────────────────────────
pymodbus==3.7.2           # Modbus TCP/RTU
pydnp3==0.1.0             # DNP3 protocol (OpenDNP3 wrapper)
paho-mqtt==2.1.0          # MQTT for IoT/AMI devices

# ── ML / AI ────────────────────────────────────────────────────────────────────
numpy==2.1.0
pandas==2.2.2
scikit-learn==1.5.2
statsmodels==0.14.2
torch==2.4.1              # PyTorch for TFT/LSTM models
# pytorch-forecasting==1.1.1  # Temporal Fusion Transformer (uncomment when needed)

# ── Auth / security ────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # password hashing
cryptography==43.0.1               # TLS, certificate handling

# ── Config / utilities ────────────────────────────────────────────────────────
pydantic==2.9.1
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2             # async HTTP client (external APIs, weather data)
aiofiles==24.1.0          # async file I/O
schedule==1.2.2           # background job scheduler

# ── Monitoring / observability ────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0
structlog==24.4.0         # structured logging
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2             # test client for FastAPI
factory-boy==3.3.1        # test data factories
